# 星载天线对地扫描三维仿真平台——需求规格与 Codex 开发说明

## 1. 项目目标

开发一个具有三维前端交互能力的星载对地观测仿真平台。平台应能够：

1. 显示可旋转、缩放和选取的三维地球。
2. 根据轨道参数生成卫星轨道、卫星位置、速度和地面航迹。
3. 显示卫星三维模型及其姿态。
4. 配置卫星天线安装方向、波束指向、波束宽度和扫描方式。
5. 计算天线波束与地球椭球面的真实交点。
6. 在地球表面显示当前瞬时照射区域和一段时间内累计覆盖区域。
7. 支持仿真时间推进、暂停、倍速、回放和参数实时修改。
8. 支持场景参数的保存、加载以及轨道、波束中心和覆盖区域数据导出。
9. 为后续接入 SAR 系统参数、回波仿真、方向图和成像算法预留接口。

本项目第一阶段的重点是“轨道—姿态—天线—地球交会—覆盖区显示”的几何正确性，而不是直接完成 SAR 回波生成或成像。

---

## 2. 推荐技术路线

### 2.1 默认推荐方案

采用 Web 三维架构：

- 前端：React + TypeScript
- 三维地球：CesiumJS
- 状态管理：Zustand 或 Redux Toolkit
- 参数面板：Ant Design
- 数值计算：TypeScript 数学模块，重计算放入 Web Worker
- 可选后端：Python + FastAPI
- 可选科学计算模块：NumPy、SciPy
- 数据格式：JSON、CSV、GeoJSON
- 桌面打包：后续可采用 Tauri 或 Electron

选择该方案的原因：

- 三维地球、经纬度、轨道线、地面多边形和相机操作可以直接实现。
- 前后端分离，后续方便接入 Python、C++、CUDA 或 MATLAB 计算模块。
- 浏览器即可运行，便于演示、部署和迭代。
- 可进一步封装为 Windows 桌面程序。

### 2.2 可选桌面方案

若必须保持 Qt/C++ 技术栈，可采用：

- Qt 6
- Qt Quick/QML
- Qt3D、OpenGL 或 VTK
- C++ 数值计算模块

但三维地球、地图瓦片、地理坐标转换和地面多边形贴附等功能需要自行完成较多基础设施，因此不建议作为首版。

---

## 3. 项目范围分级

### 3.1 MVP 必须实现

- WGS84 地球椭球模型。
- 单颗卫星。
- 圆轨道和一般开普勒椭圆轨道。
- 轨道六根数输入。
- ECI、ECEF、经纬高坐标转换。
- 地球自转。
- 卫星轨道线和地面航迹。
- 卫星 LVLH 姿态。
- 天线固定安装角和二维波束指向角。
- 圆锥波束与矩形波束。
- 波束边缘射线与地球椭球求交。
- 当前波束覆盖区。
- 累计扫描覆盖区。
- 时间轴、播放、暂停、倍速。
- 参数保存和加载。
- CSV/GeoJSON 数据导出。

### 3.2 第二阶段扩展

- 多颗卫星。
- TLE/SGP4 轨道传播。
- J2 摄动。
- 外部姿态序列。
- Stripmap、Spotlight、ScanSAR 扫描模式。
- 自定义波束扫描角时间序列。
- 数字高程模型。
- 地形遮挡。
- 二维天线方向图导入。
- 增益等值线覆盖显示。
- 多波束和阵列馈源。
- 可见性、重访时间、覆盖统计。
- 目标区域任务规划。

### 3.3 第三阶段扩展

- SAR 快时间、慢时间模型。
- PRF、带宽、脉冲宽度、载频等系统参数。
- 多普勒中心和多普勒带宽估计。
- 斜距历程。
- 回波数据生成。
- 距离模糊和方位模糊。
- 多通道 DBF、方位频谱重构。
- 成像算法接口。

---

## 4. 核心物理与几何约束

### 4.1 轨道高度和轨道速度不能随意独立设置

在“物理轨道模式”下：

- 用户输入轨道六根数，轨道位置和速度由轨道模型推导。
- 对圆轨道，速度应由以下关系计算：

\[
v=\sqrt{\frac{\mu}{R_E+h}}
\]

其中：

- \(v\)：圆轨道速度；
- \(\mu\)：地球标准引力参数；
- \(R_E\)：参考地球半径；
- \(h\)：轨道高度。

界面可以显示计算出的速度，但不应允许用户同时任意修改轨道高度和轨道速度而仍声称轨道满足真实动力学。

可另设“自由演示模式”，允许用户自定义位置、速度和轨迹，但必须在界面中明确标记为非物理模式。

### 4.2 必须区分轨道线和地面航迹

- 轨道线：卫星在惯性空间或地固空间中的运动轨迹。
- 地面航迹：卫星星下点在地球表面的轨迹。
- 地球自转开启后，两者不能简单重合。
- 地面航迹需要通过卫星 ECEF 坐标映射到 WGS84 椭球面。

### 4.3 覆盖区不能用屏幕矩形或简单投影近似

波束覆盖区必须通过以下步骤计算：

1. 在天线坐标系中生成波束中心射线。
2. 根据波束边界角生成一组边缘射线。
3. 将射线从天线坐标系转换至卫星本体坐标系。
4. 转换至 ECEF 坐标系。
5. 每条射线与 WGS84 椭球求交。
6. 将交点转换为经纬高。
7. 按边缘顺序构成地表多边形。
8. 在经度跨越 ±180° 时进行多边形拆分或经度展开处理。

若波束指向太高、指向地球外侧或没有与地球相交，则界面应显示“无地面交点”，不能生成虚假的覆盖多边形。

---

## 5. 坐标系定义

所有代码、界面提示和文档必须统一采用以下坐标系定义。

### 5.1 ECI 地心惯性坐标系

用于轨道传播。

### 5.2 ECEF 地心地固坐标系

随地球自转，用于地球表面、经纬度和波束交点计算。

### 5.3 WGS84 大地坐标

- 纬度：deg
- 经度：deg
- 高度：m

内部计算统一采用弧度和米，只有界面输入输出使用角度。

### 5.4 卫星 LVLH 坐标系

建议固定定义：

- \(+X_s\)：沿轨道速度方向，即沿航迹方向。
- \(+Z_s\)：由卫星指向地心，即天底方向。
- \(+Y_s=Z_s\times X_s\)：完成右手坐标系，表示横航迹方向。

如果轨道速度方向与径向不完全正交，应先对速度方向做正交化，再构建 LVLH 基。

### 5.5 卫星本体坐标系

首版默认与 LVLH 重合。后续通过滚转、俯仰、偏航角描述本体相对 LVLH 的姿态。

必须明确欧拉角旋转顺序，建议固定为：

\[
R_{\text{body}\leftarrow\text{LVLH}}
=R_z(\text{yaw})R_y(\text{pitch})R_x(\text{roll})
\]

代码中不得在不同模块采用不同旋转顺序。

### 5.6 天线坐标系

建议定义：

- \(+Z_a\)：天线波束中心轴。
- \(+X_a\)：天线方位平面参考方向。
- \(+Y_a\)：完成右手坐标系。

天线相对卫星本体的安装姿态由固定安装矩阵表示。

波束电子扫描角与卫星姿态角必须分开保存：

- 卫星姿态角：改变整个平台的方向。
- 天线安装角：描述天线在卫星上的固定安装关系。
- 天线扫描角：描述波束相对天线安装坐标系的动态偏转。

---

## 6. 地球模型要求

### 6.1 基础模型

- 使用 WGS84 椭球，而非仅使用球形地球。
- 支持地球贴图、经纬网和国界显示开关。
- 支持显示地心、赤道平面和地轴。
- 支持开启或关闭地球自转。
- 支持设置仿真历元。

### 6.2 可视化要求

- 鼠标拖动旋转。
- 滚轮缩放。
- 点击地球获得经纬度和高度。
- 双击定位到指定经纬度。
- 支持“跟随卫星”“跟随星下点”“自由视角”三种相机模式。
- 支持昼夜光照开关，但首版可将其作为纯显示功能，不影响计算。

---

## 7. 轨道模型要求

### 7.1 轨道输入方式

首版支持两种方式。

#### 方式 A：轨道六根数

- 半长轴 \(a\)，单位 km 或 m。
- 偏心率 \(e\)。
- 轨道倾角 \(i\)，单位 deg。
- 升交点赤经 RAAN，单位 deg。
- 近地点幅角 \(\omega\)，单位 deg。
- 真近点角 \(\nu\) 或平近点角 \(M\)，单位 deg。
- 历元时间。

#### 方式 B：简化圆轨道

- 轨道高度。
- 轨道倾角。
- 初始经度或升交点赤经。
- 初始轨道相位。
- 顺行或逆行。

系统应自动转换为内部统一的轨道状态。

### 7.2 轨道输出

实时显示：

- 当前 UTC 仿真时间。
- ECI 位置和速度。
- ECEF 位置和速度。
- 经纬度和高度。
- 瞬时轨道速度。
- 轨道周期。
- 星下点。
- 距目标点斜距。
- 目标可见性。

### 7.3 传播模型

MVP：

- 二体开普勒传播。
- ECI 到 ECEF 转换。
- 地球自转。

第二阶段：

- J2 摄动。
- TLE/SGP4。

必须将轨道传播器抽象为统一接口，避免后续更换传播模型时修改前端业务代码。

建议接口：

```ts
interface OrbitPropagator {
  propagate(epochSeconds: number): {
    positionEciM: [number, number, number];
    velocityEciMps: [number, number, number];
  };
}
```

---

## 8. 卫星模型与姿态要求

### 8.1 卫星显示

- 支持 glTF/GLB 卫星模型。
- 无模型时使用简化立方体、太阳翼和天线图标。
- 支持模型缩放。
- 支持显示速度方向、天底方向、横航迹方向。
- 支持显示卫星局部坐标轴。

### 8.2 姿态模式

MVP：

- LVLH 对地定向。
- 用户设置固定 roll、pitch、yaw 偏置。

扩展：

- 天底定向。
- 目标跟踪。
- 速度方向定向。
- 外部姿态时间序列。
- 姿态机动过程和角速度限制。

### 8.3 姿态限制

可配置：

- 最大滚转角。
- 最大俯仰角。
- 最大偏航角。
- 最大角速度。
- 最大角加速度。

首版可只做参数校验，不必完成真实姿态控制动力学。

---

## 9. 天线与波束模型要求

### 9.1 天线参数

必须支持：

- 天线名称。
- 天线安装位置，相对卫星质心的三维偏移。
- 天线安装姿态。
- 波束类型。
- 方位波束宽度。
- 俯仰波束宽度。
- 当前方位扫描角。
- 当前俯仰扫描角。
- 最大扫描角。
- 最远显示距离。
- 波束透明度。
- 波束颜色。
- 边界采样点数量。

### 9.2 波束类型

#### 圆锥波束

输入半锥角或全波束宽度。

#### 矩形角域波束

分别输入：

- 方位向全波束宽度。
- 俯仰向全波束宽度。

边缘不能只取四个角点。为了在地球曲面上形成稳定多边形，需要沿角域矩形边界采样，建议默认 64～128 个边界方向。

#### 二维方向图波束

第二阶段支持导入：

- CSV。
- JSON。
- MAT 转换后的中间格式。
- 方位角—俯仰角—增益三维表。

支持按增益门限生成覆盖区，例如：

- 3 dB 覆盖区。
- 10 dB 覆盖区。
- 用户自定义门限。

### 9.3 扫描方式

MVP：

- 固定指向。
- 手动滑块调整方位角和俯仰角。
- 正弦扫描。
- 线性往返扫描。

第二阶段：

- Stripmap：波束相对卫星保持固定。
- Spotlight：波束持续指向固定地面目标。
- ScanSAR：多个子测绘带周期切换。
- TOPS：方位向连续扫掠。
- 自定义扫描角时间表。

### 9.4 扫描时间序列

统一定义：

```ts
interface BeamSteeringLaw {
  getSteeringAngles(timeSeconds: number): {
    azimuthRad: number;
    elevationRad: number;
  };
}
```

不得将扫描规律直接写死在渲染组件中。

---

## 10. 波束与地球交会算法

### 10.1 射线方程

卫星位置为 \(\mathbf p\)，波束方向单位向量为 \(\mathbf d\)，射线为：

\[
\mathbf r(t)=\mathbf p+t\mathbf d,\quad t>0
\]

### 10.2 WGS84 椭球

椭球方程：

\[
\frac{x^2+y^2}{a^2}+\frac{z^2}{b^2}=1
\]

其中：

- \(a\)：WGS84 长半轴。
- \(b\)：WGS84 短半轴。

将射线代入椭球方程，得到关于 \(t\) 的二次方程。

处理规则：

- 判别式小于零：无交点。
- 两个根都小于零：交点在射线反方向，视为无效。
- 取最小正根作为地表交点。
- 数值上需要设置容差。
- 射线方向必须归一化。

### 10.3 当前覆盖区

- 对波束边界每一条射线求交。
- 剔除无效交点。
- 若有效交点数量不足，应判定无法形成覆盖区。
- 对极区和跨日界线场景进行经度展开。
- 覆盖多边形应贴附在地球表面，而不是悬浮在地表上方。

### 10.4 累计覆盖区

支持两种显示方式：

1. 轨迹带方式：保留每个时刻的瞬时覆盖区，形成扫描带。
2. 几何并集方式：对所有瞬时覆盖区做多边形并集。

MVP 可先实现轨迹带方式，第二阶段增加真实几何并集。

### 10.5 中心波束信息

实时计算并显示：

- 波束中心与地球交点。
- 波束中心经纬度。
- 卫星至波束中心斜距。
- 地面入射角。
- 离轴角。
- 当前地面覆盖宽度。
- 沿轨方向覆盖长度。
- 横轨方向覆盖宽度。

---

## 11. 地面目标与任务区域

### 11.1 目标类型

- 点目标。
- 圆形目标区域。
- 矩形目标区域。
- 任意多边形目标区域。
- 航迹目标，后续扩展。

### 11.2 目标输入

- 鼠标点击地球创建。
- 输入经度、纬度、高度。
- 导入 CSV。
- 导入 GeoJSON。

### 11.3 目标状态

显示：

- 是否在地平线以上。
- 是否位于当前波束覆盖区。
- 卫星至目标斜距。
- 入射角。
- 方位偏差。
- 俯仰偏差。
- 首次进入覆盖区时间。
- 离开覆盖区时间。
- 持续照射时间。

---

## 12. 前端界面布局

### 12.1 中央区域

三维地球主视图，显示：

- 地球。
- 轨道线。
- 卫星。
- 地面航迹。
- 当前波束体。
- 当前覆盖区。
- 累计覆盖区。
- 地面目标。
- 视线。
- 局部坐标轴。

### 12.2 左侧参数面板

建议使用页签：

1. 地球。
2. 轨道。
3. 卫星姿态。
4. 天线。
5. 扫描模式。
6. 目标。
7. 显示设置。
8. 数据导入导出。

参数修改分为两种：

- 可实时更新：波束角、透明度、显示开关、时间倍率。
- 需要重新初始化：历元、轨道模型、轨道六根数。

对于需要重新初始化的参数，应提供“应用”按钮，避免用户拖动输入框时反复重建场景。

### 12.3 右侧状态面板

显示实时状态：

- 仿真时间。
- 卫星经纬高。
- ECI/ECEF 坐标。
- 速度。
- 姿态角。
- 波束中心经纬度。
- 斜距。
- 入射角。
- 当前覆盖面积。
- 是否命中目标。
- 错误和警告信息。

### 12.4 底部时间轴

- 播放。
- 暂停。
- 单步前进。
- 单步后退。
- 回到初始时刻。
- 时间倍率：0.1×、1×、10×、100×、1000×。
- 可拖动时间轴。
- 仿真起止时间设置。
- 当前时间输入。

### 12.5 顶部工具栏

- 新建场景。
- 保存场景。
- 加载场景。
- 导入。
- 导出。
- 截图。
- 重置相机。
- 跟随卫星。
- 跟随波束中心。
- 帮助。

---

## 13. 交互要求

- 鼠标悬停在卫星上显示卫星状态。
- 鼠标悬停在覆盖区显示覆盖参数。
- 点击卫星自动切换到跟随视角。
- 点击地面设置目标点。
- 拖动波束角滑块时，波束体和覆盖区实时更新。
- 修改轨道参数后，轨道线、地面航迹和卫星状态同步更新。
- 支持关闭单个图层，避免画面过于拥挤。
- 所有输入框显示单位。
- 对角度输入显示合法范围。
- 对非法参数给出明确提示，不允许静默失败。

---

## 14. 数据模型

### 14.1 场景配置示例

```json
{
  "schemaVersion": "1.0",
  "earth": {
    "model": "WGS84",
    "rotationEnabled": true,
    "epochUtc": "2026-07-15T00:00:00Z"
  },
  "simulation": {
    "startUtc": "2026-07-15T00:00:00Z",
    "durationSeconds": 5400,
    "timeStepSeconds": 1,
    "playbackRate": 100
  },
  "orbit": {
    "type": "keplerian",
    "semiMajorAxisM": 6878137,
    "eccentricity": 0,
    "inclinationDeg": 97.4,
    "raanDeg": 0,
    "argumentOfPerigeeDeg": 0,
    "trueAnomalyDeg": 0
  },
  "attitude": {
    "mode": "LVLH",
    "rollDeg": 0,
    "pitchDeg": 0,
    "yawDeg": 0
  },
  "antenna": {
    "beamType": "rectangular",
    "mountRollDeg": 0,
    "mountPitchDeg": 0,
    "mountYawDeg": 0,
    "steeringAzimuthDeg": 0,
    "steeringElevationDeg": 35,
    "azimuthBeamwidthDeg": 2,
    "elevationBeamwidthDeg": 6,
    "boundarySamples": 96
  }
}
```

### 14.2 仿真结果数据

每个时间步至少包含：

```ts
interface SimulationSample {
  timeUtc: string;
  positionEciM: [number, number, number];
  velocityEciMps: [number, number, number];
  positionEcefM: [number, number, number];
  longitudeDeg: number;
  latitudeDeg: number;
  altitudeM: number;
  attitudeQuaternion: [number, number, number, number];
  beamCenterLongitudeDeg?: number;
  beamCenterLatitudeDeg?: number;
  slantRangeM?: number;
  incidenceAngleDeg?: number;
  footprintGeoJson?: object;
}
```

---

## 15. 软件架构

建议目录结构：

```text
satellite-scan-simulator/
├─ apps/
│  ├─ web/
│  │  ├─ src/
│  │  │  ├─ components/
│  │  │  ├─ panels/
│  │  │  ├─ viewer/
│  │  │  ├─ stores/
│  │  │  ├─ hooks/
│  │  │  └─ workers/
│  │  └─ public/
│  └─ server/
│     ├─ app/
│     └─ tests/
├─ packages/
│  ├─ orbital-core/
│  │  ├─ coordinates/
│  │  ├─ propagators/
│  │  ├─ attitude/
│  │  ├─ antenna/
│  │  ├─ intersection/
│  │  └─ coverage/
│  ├─ shared-types/
│  └─ test-fixtures/
├─ docs/
├─ examples/
└─ README.md
```

核心模块职责：

- `coordinates`：ECI、ECEF、WGS84、ENU 转换。
- `propagators`：轨道传播。
- `attitude`：LVLH、本体姿态、四元数。
- `antenna`：安装矩阵、扫描规律、波束边界。
- `intersection`：射线与 WGS84 椭球求交。
- `coverage`：瞬时覆盖区、累计覆盖区、命中判断。
- `viewer`：只负责渲染，不实现轨道和几何公式。
- `stores`：管理 UI 状态和场景配置。
- `workers`：后台计算轨道采样和覆盖区。

必须保证“计算层”和“渲染层”分离。禁止在 Cesium 组件内部直接堆叠所有轨道、姿态和求交公式。

---

## 16. 性能要求

MVP 目标：

- 常规桌面浏览器中保持可交互。
- 动画时视觉帧率目标不低于 30 FPS。
- 轨道预计算和覆盖区计算不能长期阻塞主线程。
- 轨道采样点 10000 个时仍可正常缩放和旋转。
- 覆盖区历史过多时采用抽样、分段或按时间窗口显示。
- 参数拖动时采用节流或防抖。
- 内部计算与视觉帧率解耦。
- 仿真时间步不应由浏览器每帧时间直接累加，必须由统一仿真时钟控制。

---

## 17. 数值与工程规范

- 长度统一使用 m。
- 速度统一使用 m/s。
- 时间统一使用 s 和 UTC ISO 8601。
- 内部角度统一使用 rad。
- UI 角度使用 deg。
- 禁止在核心计算函数中混用 km 和 m。
- 禁止直接比较浮点数是否相等。
- 所有旋转矩阵和四元数必须有归一化检查。
- 射线方向必须归一化。
- 所有公共函数应有输入范围说明。
- 核心数学函数应为纯函数，便于单元测试。
- 保存文件中必须包含 `schemaVersion`。
- 配置升级时需要兼容旧版本或给出迁移提示。

---

## 18. 异常处理

需要识别并显示：

- 轨道参数非法。
- 偏心率超出有效范围。
- 半长轴小于地球半径。
- 卫星位于地球内部。
- 波束没有与地球相交。
- 波束覆盖区跨越日界线。
- 覆盖区经过极点。
- 姿态矩阵无效。
- 参数单位错误。
- 导入文件字段缺失。
- 时间范围过大。
- 采样步长过小导致计算量过大。
- 浏览器 WebGL 不可用。

错误信息必须说明原因和修改建议，不能只显示“计算失败”。

---

## 19. 单元测试与验证要求

### 19.1 坐标转换测试

- 经纬高转 ECEF 后再反算，经纬度和高度误差应在设定容差内。
- 赤道、北极、南极和日界线附近均需测试。

### 19.2 轨道测试

- 圆轨道传播一周后，位置应回到初始位置附近。
- 圆轨道半径和速度模长应基本保持恒定。
- 开启地球自转后，地面航迹不能与惯性轨道投影完全相同。

### 19.3 姿态测试

- LVLH 的三个轴应正交且归一。
- \(X_s\times Y_s=Z_s\)。
- 零姿态偏置时本体坐标系与 LVLH 重合。

### 19.4 波束求交测试

- 天底指向时，中心交点应接近星下点。
- 波束指向地球外侧时应返回无交点。
- 对称波束在天底附近应形成近似对称覆盖区。
- 所有有效交点代入椭球方程后应接近 1。

### 19.5 UI 测试

- 修改参数后显示状态同步更新。
- 保存后重新加载应恢复同一场景。
- 时间轴暂停后卫星不能继续移动。
- 切换相机跟随模式不会改变物理计算结果。

---

## 20. 验收标准

项目至少满足以下条件才算 MVP 完成：

1. 用户可以修改轨道高度、倾角、RAAN 和初始相位，并看到轨道与地面航迹更新。
2. 卫星能够沿轨道随时间运动。
3. 地球自转对地面航迹产生正确影响。
4. 用户可以修改卫星姿态和天线扫描角。
5. 界面显示真实三维波束体。
6. 波束与 WGS84 椭球求交得到地表覆盖区。
7. 当前覆盖区随卫星运动连续变化。
8. 可显示一段时间内形成的扫描带。
9. 点击地面可以添加目标，并判断目标是否进入覆盖区。
10. 仿真可以播放、暂停、单步和改变速度。
11. 场景可以保存为 JSON 并重新加载。
12. 轨道、波束中心和覆盖区可以导出。
13. 核心坐标、轨道和求交模块具有单元测试。
14. 无交点、参数非法和跨日界线等情况有明确处理。
15. README 中提供安装、启动、操作和数学模型说明。

---

## 21. 推荐开发顺序

### 里程碑 1：基础三维地球

- 创建 React + TypeScript 项目。
- 集成 Cesium。
- 显示地球和基本 UI。
- 实现相机控制和坐标拾取。

### 里程碑 2：轨道与卫星

- 实现轨道六根数数据结构。
- 实现二体轨道传播。
- 实现 ECI/ECEF/WGS84 转换。
- 显示轨道线、卫星和地面航迹。
- 实现仿真时钟。

### 里程碑 3：姿态和天线

- 构建 LVLH 坐标系。
- 实现本体姿态。
- 实现天线安装矩阵。
- 显示波束中心轴和波束体。

### 里程碑 4：覆盖区计算

- 实现 WGS84 射线求交。
- 实现圆锥和矩形波束边界采样。
- 生成当前覆盖区。
- 处理无交点、日界线和极区。

### 里程碑 5：扫描与目标

- 实现固定、线性和正弦扫描。
- 实现目标点。
- 实现目标命中和持续照射时间统计。
- 显示累计扫描带。

### 里程碑 6：工程化

- 保存和加载。
- 导入和导出。
- Web Worker。
- 单元测试。
- 文档。
- 示例场景。
- 桌面打包可行性验证。

---

## 22. Codex 开发规则

将以下规则作为 Codex 的长期约束：

1. 每次只完成一个里程碑或一个可验证功能。
2. 修改代码前先阅读项目结构和已有 README。
3. 不得将所有逻辑写入单个 React 组件。
4. 轨道、坐标、姿态和射线求交必须放在独立数学模块。
5. 每增加一个核心数学函数，同时增加单元测试。
6. 所有物理量的单位必须写入类型名、变量名或注释。
7. 内部使用弧度和米。
8. 不得用球面矩形假装真实波束覆盖区。
9. 不得把轨道高度和速度同时当作独立物理输入。
10. 不得把 ECI、ECEF 和经纬度坐标混在同一变量中。
11. 不得让渲染帧率决定仿真时间。
12. 遇到不确定的角度定义时，先在文档中明确坐标系和正方向，再编码。
13. 每个里程碑完成后运行测试、构建项目并更新 README。
14. 不得删除已通过的测试以绕过错误。
15. 优先实现正确、可测试的模型，再优化视觉效果。

---

## 23. 可直接提交给 Codex 的首轮任务

```text
你需要创建一个“星载天线对地扫描三维仿真平台”的 MVP。

技术栈：
- React
- TypeScript
- Vite
- CesiumJS
- Ant Design
- Zustand
- Vitest

本轮只完成里程碑 1 和里程碑 2，不要提前实现天线波束。

功能要求：
1. 显示可旋转、缩放的三维 WGS84 地球。
2. 点击地球后显示点击位置的经度、纬度和高度。
3. 实现统一仿真时钟，支持播放、暂停、单步、重置和倍率调整。
4. 支持简化圆轨道参数：
   - 轨道高度
   - 轨道倾角
   - RAAN
   - 初始轨道相位
   - 仿真历元
5. 根据圆轨道参数计算轨道速度，不能让高度和速度作为相互独立的物理输入。
6. 实现二体圆轨道传播。
7. 实现 ECI 到 ECEF 的转换，并考虑地球自转。
8. 实现 ECEF 到 WGS84 经纬高转换。
9. 在 Cesium 中显示：
   - 三维轨道线
   - 卫星模型或占位模型
   - 地面航迹
   - 当前星下点
10. 右侧状态面板显示：
   - 当前 UTC 时间
   - 卫星经度、纬度、高度
   - ECI 位置和速度
   - ECEF 位置
   - 瞬时速度
   - 轨道周期
11. 代码必须分层：
   - orbital-core/coordinates
   - orbital-core/propagators
   - orbital-core/time
   - viewer
   - panels
   - stores
12. 核心数学代码不得写在 React 组件中。
13. 为坐标转换、圆轨道速度和轨道周期增加 Vitest 单元测试。
14. 内部统一使用米、秒、弧度，界面显示使用千米和角度。
15. 创建 README，说明安装、启动、坐标系、单位和已实现功能。
16. 完成后执行测试和生产构建，修复所有错误。
17. 最后汇报：
   - 创建和修改的文件
   - 采用的数学模型
   - 测试结果
   - 当前限制
   - 下一步建议

请先输出实施计划和预计文件结构，然后再开始创建代码。
```

---

## 24. 第二轮可提交给 Codex 的任务

```text
在现有星载仿真平台上实现卫星姿态、天线波束和地面覆盖区。

要求：
1. 建立卫星 LVLH 坐标系：
   - +X 沿速度方向
   - +Z 指向地心
   - +Y = +Z × +X
2. 支持 roll、pitch、yaw 姿态偏置，旋转顺序固定为 Rz(yaw)Ry(pitch)Rx(roll)。
3. 建立独立天线坐标系，+Z 为波束中心轴。
4. 支持天线固定安装姿态。
5. 支持天线方位扫描角和俯仰扫描角。
6. 支持圆锥波束和矩形角域波束。
7. 根据波束边界方向生成 64～128 条边缘射线。
8. 将边缘射线转换至 ECEF。
9. 实现射线与 WGS84 椭球求交。
10. 使用最小正根作为地表交点。
11. 将交点构造成 Cesium 地表多边形。
12. 显示：
    - 波束中心轴
    - 半透明波束体
    - 当前瞬时覆盖区
    - 波束中心地面点
13. 实时显示：
    - 波束中心经纬度
    - 斜距
    - 入射角
    - 横轨覆盖宽度
    - 沿轨覆盖长度
14. 处理以下异常：
    - 无地球交点
    - 波束指向地球外侧
    - 跨 ±180° 日界线
    - 极区多边形
15. 为射线椭球求交、LVLH 正交性、天底指向等增加单元测试。
16. 不允许使用屏幕空间矩形或固定地面矩形替代真实覆盖区。
17. 完成后运行测试和构建，并更新 README。
```

---

## 25. 开发前仍需确认的产品决策

交付 Codex 前，建议明确以下问题：

1. 最终是浏览器应用还是必须提供 Windows 安装包。
2. 首版是否只考虑单颗卫星。
3. 首版是否允许只使用二体轨道。
4. 是否必须支持 TLE。
5. 地球是否需要真实地形。
6. 当前覆盖区只显示主瓣边界，还是显示二维增益等值线。
7. 天线波束宽度采用全角还是半角。
8. 天线扫描角的零方向和正方向。
9. 卫星姿态角采用何种旋转顺序。
10. 是否需要 Stripmap、Spotlight、ScanSAR 和 TOPS。
11. 是否要显示累计覆盖区的真实并集面积。
12. 是否需要导入外部卫星三维模型。
13. 是否需要导入真实天线方向图。
14. 是否需要与 MATLAB、Python 或 C++ 回波程序通信。
15. 最终是否要扩展到 SAR 原始回波和成像。

建议首版明确限制为：

- 单颗卫星。
- WGS84 椭球。
- 二体轨道。
- LVLH 对地定向。
- 圆锥和矩形波束。
- 固定与简单周期扫描。
- 不考虑地形。
- 不考虑大气。
- 不生成 SAR 回波。
- 重点验证坐标、轨道、姿态和覆盖区几何。
